spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          default-label: main
          clone-on-start: true
          uri: https://github.com/BhushanGhodke/Config_Server_Banking_Project.git
  
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/


server:
  port: 4000